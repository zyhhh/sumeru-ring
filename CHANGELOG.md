# 变更记录

本项目遵循 Keep a Changelog 的组织方式。

## 未发布

### 新增

- 确定 SUMERU RING 首期产品、架构、技术选型与视觉规范。
- 规划 JWT、二维码、JSON、URL 和字符统计五个纯前端工具。
- 初始化 Vue 3、TypeScript、Vite、Element Plus 应用工程。
- 新增可折叠导航、全局搜索、明暗主题、标签筛选和注册式工具首页。
- 新增 JWT 解析与 HS256/HS384/HS512 签名验证。
- 新增支持结构化内容、Logo 和样式定制的二维码生成器，以及本地图片识别。
- 新增通过 Web Worker 处理的 JSON 格式化、压缩、转义和去转义工具。
- 新增完整 URL 与 URL 组件编解码工具。
- 新增字符分类统计、原始字符数与加权字符数计算。
- 新增核心逻辑单元测试、开发指南与 Nginx 部署文档。
- 设计基于 IndexedDB 的网址导航与管理功能，包含初始化数据、分类、导入导出和恢复默认规则。
- 新增工具、中转站、资源和影视四类初始化网址。
- 新增网址管理页面，支持新增、编辑、删除、搜索、分类筛选、排序、JSON 导入导出和恢复默认网址。
- 首页和全局搜索接入 IndexedDB 网址数据，外部网址使用安全的新标签页打开方式。
- 新增独立小工具列表页与网址导航页，均支持每页 16 项分页。
- 新增首页内容类型概览，每类最多展示 5 项，并在内容超过 5 项时提供“查看更多”入口。
- 新增 Git 风格文本比较工具，支持行内高亮、同步滚动、忽略空白、差异统计和逐项跳转。
- 新增时间戳转换工具，支持秒、毫秒、ISO 8601 和本地时间互转。
- 新增 UTF-8 Base64 编解码工具。
- 新增 UUID v4 与雪花 ID 批量生成工具。
- 新增 MD5、SHA-1、SHA-256、SHA-512 与 HMAC 计算工具。
- 新增 Cron 表达式校验、说明与未来执行时间预览工具。

### 调整

- 统一放大正文、标签、字段标题和使用说明字号，改善长时间使用时的可读性。
- 将顶部搜索改为图标触发展开，并优化搜索结果浮层。
- 统一主要、次要、单选和上传按钮的尺寸、圆角、颜色与交互反馈。
- 将全站命令按钮、标签筛选、分段选择和文件上传统一迁移到 Element Plus，消除不同页面的控件割裂感。
- 更新新增工具指南，补充 Element Plus 组件映射、上传约束、视觉规范和验证流程。
- 拆分 `/`、`/tools`、`/links` 与 `/links/manage` 的页面职责，修复“全部内容”和“小工具”同时高亮的问题。
- 将侧栏“常用网址”统一更名为“网址导航”，并将网址管理入口移动到网址导航页右上角。
